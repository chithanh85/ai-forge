# Run tests before push
npm run test

# Secret leak scan
echo "🔍 Scanning for leaked secrets..."
if git diff --cached --name-only | xargs grep -lE '(ghp_[a-zA-Z0-9]{36}|sk-[a-zA-Z0-9]{48}|AKIA[A-Z0-9]{16}|password\s*=\s*"[^"]+")' 2>/dev/null; then
  echo "❌ BLOCKED: Secret pattern detected in staged files!"
  exit 1
fi
echo "✅ No secrets detected"
