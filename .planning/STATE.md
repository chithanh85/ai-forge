# Project State ? AI reads this FIRST at every session

## Current Phase

- [x] AWF template source ready for project initialization
- [ ] Project planning
- [ ] Design
- [ ] Implementation
- [ ] Testing
- [ ] Deployment

## Template Source

This repository is the AWF distribution source. Project-specific identity and session state are generated by `node scripts/awf/init.mjs --project-name <name>`.

Canonical project configuration: `.awf/manifest.json`.
Canonical client-neutral policy: `.awf/policy/core.md`.
Client adapters: `AGENTS.md`, `GEMINI.md`, and `CLAUDE.md`.

## New Project Flow

1. Run the platform setup script or `node scripts/awf/init.mjs --project-name <name>`.
2. Optional integrations remain opt-in and capability-detected.
3. Run `node scripts/awf/doctor.mjs` to diagnose configuration drift.
4. Run `/brainstorm`, `/ba-pipeline`, or the project workflow required by the task.

## Last Session

_No project session is shipped with the template._

## Active Tasks

_None. Template-maintainer history belongs in Git/docs, not project runtime state._

## Known Issues

_None recorded for the generated project._
